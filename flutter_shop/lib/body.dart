import 'package:flutter/material.dart';

// building the text section after the appbar

class Body extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Column(children: [],);
  }

}